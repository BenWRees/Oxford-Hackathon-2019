
#encapsulates the data inputted from the user
class UserData :
	def __init__(self, location,distance,keyword="bar") :
		self.location = location
		self.distance = distance
		self.keyword = keyword

	def getLocation(self) :
		return location

	def getDistance(self) :
		return distance

	def getKeyword(self)
		return keyword
